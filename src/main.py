[
  {
    "path": "requirements.txt",
    "content": "pywin32>=306\npsutil>=5.9.0\nPillow>=10.0.0\nopencv-python>=4.8.0\nnumpy>=1.24.0"
  },
  {
    "path": "src/__init__.py",
    "content": ""
  },
  {
    "path": "src/memory_reader.py",
    "content": "\"\"\"\nMemory reader module for Clair Obscur: Expedition 33 Trainer.\nUses Windows API via pywin32 to read/write process memory.\n\"\"\"\n\nimport ctypes\nfrom ctypes import wintypes\nfrom typing import Optional, List, Tuple\n\n# Windows API constants\nPROCESS_ALL_ACCESS = 0x1F0FFF\nPROCESS_VM_READ = 0x0010\nPROCESS_VM_WRITE = 0x0020\nPROCESS_VM_OPERATION = 0x0008\n\n# Load kernel32.dll for memory operations\nkernel32 = ctypes.WinDLL('kernel32', use_last_error=True)\n\n\nclass MemoryReader:\n    \"\"\"Handles reading and writing process memory for the trainer.\"\"\"\n\n    def __init__(self, process_name: str = \"ClairObscur-Win64-Shipping.exe\"):\n        self.process_name = process_name\n        self.process_handle = None\n        self.pid = None\n\n    def open_process(self) -> bool:\n        \"\"\"Open a handle to the target process by name.\"\"\"\n        import psutil\n        for proc in psutil.process_iter(['pid', 'name']):\n            if proc.info['name'] == self.process_name:\n                self.pid = proc.info['pid']\n                break\n        if self.pid is None:\n            return False\n\n        self.process_handle = kernel32.OpenProcess(\n            PROCESS_ALL_ACCESS,\n            False,\n            self.pid\n        )\n        return self.process_handle is not None\n\n    def close_process(self) -> None:\n        \"\"\"Close the process handle.\"\"\"\n        if self.process_handle:\n            kernel32.CloseHandle(self.process_handle)\n            self.process_handle = None\n\n    def read_bytes(self, address: int, size: int) -> Optional[bytes]:\n        \"\"\"Read bytes from process memory at given address.\"\"\"\n        buffer = ctypes.create_string_buffer(size)\n        bytes_read = ctypes.c_size_t(0)\n        success = kernel32.ReadProcessMemory(\n            self.process_handle,\n            ctypes.c_void_p(address),\n            buffer,\n            size,\n            ctypes.byref(bytes_read)\n        )\n        if success and bytes_read.value == size:\n            return buffer.raw\n        return None\n\n    def write_bytes(self, address: int, data: bytes) -> bool:\n        \"\"\"Write bytes to process memory at given address.\"\"\"\n        bytes_written = ctypes.c_size_t(0)\n        success = kernel32.WriteProcessMemory(\n            self.process_handle,\n            ctypes.c_void_p(address),\n            data,\n            len(data),\n            ctypes.byref(bytes_written)\n        )\n        return success and bytes_written.value == len(data)\n\n    def read_int32(self, address: int) -> Optional[int]:\n        \"\"\"Read a 32-bit integer from memory.\"\"\"\n        data = self.read_bytes(address, 4)\n        if data:\n            return int.from_bytes(data, 'little', signed=True)\n        return None\n\n    def write_int32(self, address: int, value: int) -> bool:\n        \"\"\"Write a 32-bit integer to memory.\"\"\"\n        data = value.to_bytes(4, 'little', signed=True)\n        return self.write_bytes(address, data)\n\n    def read_float(self, address: int) -> Optional[float]:\n        \"\"\"Read a 32-bit float from memory.\"\"\"\n        data = self.read_bytes(address, 4)\n        if data:\n            import struct\n            return struct.unpack('<f', data)[0]\n        return None\n\n    def write_float(self, address: int, value: float) -> bool:\n        \"\"\"Write a 32-bit float to memory.\"\"\"\n        import struct\n        data = struct.pack('<f', value)\n        return self.write_bytes(address, data)\n\n    def find_signature(self, pattern: bytes, mask: str, start: int = 0x400000, end: int = 0x7FFFFFFF) -> List[int]:\n        \"\"\"\n        Scan memory for a byte pattern with mask (AOB scan).\n        'x' in mask means byte must match, '?' means wildcard.\n        \"\"\"\n        results = []\n        chunk_size = 4096\n        for addr in range(start, end, chunk_size):\n            chunk = self.read_bytes(addr, chunk_size)\n            if not chunk:\n                continue\n            for i in range(len(chunk) - len(pattern) + 1):\n                match = True\n                for j in range(len(pattern)):\n                    if mask[j] == 'x' and chunk[i + j] != pattern[j]:\n                        match = False\n                        break\n                if match:\n                    results.append(addr + i)\n        return results\n"
  },
  {
    "path": "src/trainer_core.py",
    "content": "\"\"\"\nCore trainer logic for Clair Obscur: Expedition 33.\nProvides cheats like infinite health, infinite stamina, and item editing.\n\"\"\"\n\nimport logging\nimport time\nfrom typing import Dict, Optional, Callable\nfrom src.memory_reader import MemoryReader\n\nlogger = logging.getLogger(__name__)\n\n\nclass TrainerCore:\n    \"\"\"Main trainer class that manages cheat features.\"\"\"\n\n    # Known memory offsets for Clair Obscur: Expedition 33 (example values)\n    # In a real trainer, these would be discovered via reverse engineering\n    OFFSETS = {\n        \"health\": 0x00A1B2C0,\n        \"max_health\": 0x00A1B2C4,\n        \"stamina\": 0x00A1B2D0,\n        \"max_stamina\": 0x00A1B2D4,\n        \"expedition_points\": 0x00A1B2E0,\n        \"current_ammo\": 0x00A1B2F0,\n        \"max_ammo\": 0x00A1B2F4,\n    }\n\n    def __init__(self):\n        self.memory = MemoryReader()\n        self.active_cheats: Dict[str, bool] = {\n            \"infinite_health\": False,\n            \"infinite_stamina\": False,\n            \"infinite_ammo\": False,\n            \"max_expedition_points\": False,\n        }\n        self._running = False\n        self._loop_thread = None\n\n    def attach(self) -> bool:\n        \"\"\"Attach to the game process.\"\"\"\n        logger.info(\"Attempting to attach to Clair Obscur: Expedition 33...\")\n        if self.memory.open_process():\n            logger.info(f\"Attached to process PID: {self.memory.pid}\")\n            return True\n        logger.error(\"Failed to attach to game process. Is the game running?\")\n        return False\n\n    def detach(self) -> None:\n        \"\"\"Detach from the game process.\"\"\"\n        self.stop_all()\n        self.memory.close_process()\n        logger.info(\"Detached from game process.\")\n\n    def set_health(self, value: float) -> bool:\n        \"\"\"Set player health to a specific value.\"\"\"\n        addr = self.OFFSETS[\"health\"]\n        success = self.memory.write_float(addr, value)\n        if success:\n            logger.debug(f\"Health set to {value}\")\n        return success\n\n    def set_stamina(self, value: float) -> bool:\n        \"\"\"Set player stamina to a specific value.\"\"\"\n        addr = self.OFFSETS[\"stamina\"]\n        success = self.memory.write_float(addr, value)\n        if success:\n            logger.debug(f\"Stamina set to {value}\")\n        return success\n\n    def set_expedition_points(self, value: int) -> bool:\n        \"\"\"Set expedition points (currency) to a specific value.\"\"\"\n        addr = self.OFFSETS[\"expedition_points\"]\n        success = self.memory.write_int32(addr, value)\n        if success:\n            logger.debug(f\"Expedition points set to {value}\")\n        return success\n\n    def set_ammo(self, value: int) -> bool:\n        \"\"\"Set current ammo count.\"\"\"\n        addr = self.OFFSETS[\"current_ammo\"]\n        success = self.memory.write_int32(addr, value)\n        if success:\n            logger.debug(f\"Ammo set to {value}\")\n        return success\n\n    def enable_cheat(self, cheat_name: str) -> bool:\n        \"\"\"Enable a specific cheat by name.\"\"\"\n        if cheat_name not in self.active_cheats:\n            logger.warning(f\"Unknown cheat: {cheat_name}\")\n            return False\n        self.active_cheats[cheat_name] = True\n        logger.info(f\"Cheat '{cheat_name}' enabled.\")\n        return True\n\n    def disable_cheat(self, cheat_name: str) -> bool:\n        \"\"\"Disable a specific cheat by name.\"\"\"\n        if cheat_name not in self.active_cheats:\n            logger.warning(f\"Unknown cheat: {cheat_name}\")\n            return False\n        self.active_cheats[cheat_name] = False\n        logger.info(f\"Cheat '{cheat_name}' disabled.\")\n        return True\n\n    def stop_all(self) -> None:\n        \"\"\"Disable all active cheats.\"\"\"\n        for cheat in self.active_cheats:\n            self.active_cheats[cheat] = False\n        logger.info(\"All cheats disabled.\")\n\n    def _cheat_loop(self) -> None:\n        \"\"\"Main loop that applies active cheats every 100ms.\"\"\"\n        while self._running:\n            try:\n                if self.active_cheats[\"infinite_health\"]:\n                    max_hp = self.memory.read_float(self.OFFSETS[\"max_health\"])\n                    if max_hp is not None:\n                        self.set_health(max_hp)\n\n                if self.active_cheats[\"infinite_stamina\"]:\n                    max_stam = self.memory.read_float(self.OFFSETS[\"max_stamina\"])\n                    if max_stam is not None:\n                        self.set_stamina(max_stam)\n\n                if self.active_cheats[\"infinite_ammo\"]:\n                    max_ammo = self.memory.read_int32(self.OFFSETS[\"max_ammo\"])\n                    if max_ammo is not None:\n                        self.set_ammo(max_ammo)\n\n                if self.active_cheats[\"max_expedition_points\"]:\n                    self.set_expedition_points(99999)\n\n            except Exception as e:\n                logger.error(f\"Error in cheat loop: {e}\")\n            time.sleep(0.1)\n\n    def start_cheat_loop(self) -> None:\n        \"\"\"Start the background cheat application loop.\"\"\"\n        if self._running:\n            return\n        self._running = True\n        import threading\n        self._loop_thread = threading.Thread(target=self._cheat_loop, daemon=True)\n        self._loop_thread.start()\n        logger.info(\"Cheat loop started.\")\n\n    def stop_cheat_loop(self) -> None:\n        \"\"\"Stop the background cheat application loop.\"\"\"\n        self._running = False\n        if self._loop_thread:\n            self._loop_thread.join(timeout=1.0)\n        logger.info(\"Cheat loop stopped.\")\n\n    def get_status(self) -> Dict[str, bool]:\n        \"\"\"Return current status of all cheats.\"\"\"\n        return dict(self.active_cheats)\n"
  },
  {
    "path": "src/overlay.py",
    "content": "\"\"\"\nSimple overlay module using Pillow to display trainer status.\nIn a real trainer, this would be a transparent window overlay.\n\"\"\"\n\nimport logging\nfrom typing import Dict\n\nlogger = logging.getLogger(__name__)\n\n\nclass Overlay:\n    \"\"\"Handles display overlay for the trainer (placeholder implementation).\"\"\"\n\n    def __init__(self):\n        self.visible = False\n        self.position = (100, 100)\n        self.font_size = 14\n\n    def show(self) -> None:\n        \"\"\"Show the overlay.\"\"\"\n        self.visible = True\n        logger.info(\"Overlay shown.\")\n\n    def hide(self) -> None:\n        \"\"\"Hide the overlay.\"\"\"\n        self.visible = False\n        logger.info(\"Overlay hidden.\")\n\n    def update_status(self, cheats: Dict[str, bool]) -> None:\n        \"\"\"Update the displayed cheat status.\"\"\"\n        if not self.visible:\n            return\n        # In a full implementation, this would render text on a transparent window\n        lines = []\n        for cheat, enabled in cheats.items():\n            status = \"ON\" if enabled else \"OFF\"\n            lines.append(f\"{cheat}: {status}\")\n        display_text = \"\\n\".join(lines)\n        logger.debug(f\"Overlay updated:\\n{display_text}\")\n\n    def draw(self) -> None:\n        \"\"\"Render the overlay frame (called each frame).\"\"\"\n        if not self.visible:\n            return\n        # Placeholder: in real implementation, use Pillow/PyQt to draw on screen\n        pass\n"
  },
  {
    "path": "tests/__init__.py",
    "content": ""
  },
  {
    "path": "tests/test_trainer_core.py",
    "content": "\"\"\"\nUnit tests for TrainerCore functionality.\nUses mocking to avoid requiring actual game process.\n\"\"\"\n\nimport unittest\nfrom unittest.mock import patch, MagicMock\nfrom src.trainer_core import TrainerCore\n\n\nclass TestTrainerCore(unittest.TestCase):\n    \"\"\"Test cases for TrainerCore class.\"\"\"\n\n    def setUp(self):\n        self.trainer = TrainerCore()\n\n    def test_initial_cheats_disabled(self):\n        \"\"\"All cheats should be disabled by default.\"\"\"\n        status = self.trainer.get_status()\n        for cheat, enabled in status.items():\n            with self.subTest(cheat=cheat):\n                self.assertFalse(enabled, f\"Cheat '{cheat}' should be disabled initially\")\n\n    def test_enable_cheat_valid(self):\n        \"\"\"Enabling a valid cheat should set it to True.\"\"\"\n        result = self.trainer.enable_cheat(\"infinite_health\")\n        self.assertTrue(result)\n        self.assertTrue(self.trainer.active_cheats[\"infinite_health\"])\n\n    def test_enable_cheat_invalid(self):\n        \"\"\"Enabling an invalid cheat should return False.\"\"\"\n        result = self.trainer.enable_cheat(\"nonexistent_cheat\")\n        self.assertFalse(result)\n\n    def test_disable_cheat_valid(self):\n        \"\"\"Disabling a valid cheat should set it to False.\"\"\"\n        self.trainer.enable_cheat(\"infinite_stamina\")\n        result = self.trainer.disable_cheat(\"infinite_stamina\")\n        self.assertTrue(result)\n        self.assertFalse(self.trainer.active_cheats[\"infinite_stamina\"])\n\n    def test_disable_cheat_invalid(self):\n        \"\"\"Disabling an invalid cheat should return False.\"\"\"\n        result = self.trainer.disable_cheat(\"fake_cheat\")\n        self.assertFalse(result)\n\n    def test_stop_all_disables_all(self):\n        \"\"\"stop_all should disable all cheats.\"\"\"\n        self.trainer.enable_cheat(\"infinite_health\")\n        self.trainer.enable_cheat(\"infinite_ammo\")\n        self.trainer.stop_all()\n        status = self.trainer.get_status()\n        for cheat, enabled in status.items():\n            with self.subTest(cheat=cheat):\n                self.assertFalse(enabled, f\"Cheat '{cheat}' should be disabled after stop_all\")\n\n    @patch('src.trainer_core.MemoryReader')\n    def test_set_health(self, mock_memory_reader):\
